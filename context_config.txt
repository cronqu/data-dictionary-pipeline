# context_config.txt  — Edit this file ONCE to describe your study and dataset.
# This context is injected into ai_input.txt and passed to the AI tool along
# with the column list and your reference document.
#
# Keep each section brief (2–5 sentences). Do NOT include participant names,
# record IDs, data values, or any information that could identify individuals.
#
# Lines beginning with # are comments and are ignored by the pipeline scripts.
# ─────────────────────────────────────────────────────────────────────────────

STUDY_NAME: [Enter your study or project name here]

STUDY_CONTEXT:
[Describe what this dataset is about in 2–5 sentences. Include:
 - The purpose or research question of the study
 - The approximate time period the data covers
 - The population or setting (e.g., "adult outpatients at a regional health authority")
 - Any key outcomes or variables of interest
 Do NOT include institution names, participant IDs, or real data values.]

DATA_SOURCES:
[List the source systems or databases the data came from. For each source,
 give its common name and a brief description of what it contains.
 Example format:
   - [System A name]: [what it contains, e.g., administrative records, clinical assessments]
   - [System B name]: [what it contains]
 If data comes from a single source, list just that one.]

TARGET_VARIABLE:
[If applicable, describe the primary outcome or target variable — the column
 your analysis is trying to predict or explain. Include the column name and
 what a positive value means. If there is no single target variable, write
 "Not applicable — exploratory / descriptive analysis."]
